# Variables de entorno para Railway (sin usar la sintaxis de interpolación ${{VARIABLE}})
# Copiar estos valores directamente en las variables de entorno de Railway

# Variables de base de datos
MYSQLHOST=<El valor real de RAILWAY_PRIVATE_DOMAIN sin ${{}}> 
MYSQLPORT=3306
MYSQLUSER=root
MYSQLPASSWORD=<El valor real de MYSQL_ROOT_PASSWORD sin ${{}}> 
MYSQLDATABASE=railway

# Variables de aplicación
NODE_ENV=production
TZ=America/La_Paz

# Variable para healthcheck
RAILWAY_HEALTHCHECK=true
